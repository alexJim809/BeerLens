<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BeerLens" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorAccent">@color/yellow</item>
        <item name="android:windowLightStatusBar">true</item> <!-- Iconos oscuros en la barra de estado -->
    </style>

    <style name="AlertDialogCustom" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:buttonBarNegativeButtonStyle">@style/NegativeButtonStyle</item>
        <item name="android:buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
    </style>

    <style name="NegativeButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="PositiveButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="Base.Theme.BeerLens.description" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">#FF9800</item>
        <item name="colorAccent">@color/yellow</item>
        <item name="android:windowLightStatusBar">true</item> <!-- Iconos oscuros en la barra de estado -->

    </style>

    <style name="Theme.BeerLens" parent="Base.Theme.BeerLens" />
    <style name="SplashScreenTheme" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowBackground">@color/white</item> <!-- Fondo blanco -->
        <item name="android:statusBarColor">@color/white</item> <!-- Color de la barra de estado blanco -->
        <item name="android:windowLightStatusBar">true</item> <!-- Iconos oscuros en la barra de estado -->
    </style>
</resources>